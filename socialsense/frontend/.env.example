VITE_API_URL=http://localhost:3001/api

# Required only for login and the original authenticated SaaS workflow.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=

# Shows LLM controls in the original authenticated workflow. The public demo
# does not require this flag.
VITE_AI_ENABLED=true
