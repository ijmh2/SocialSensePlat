VITE_API_URL=http://localhost:3001/api

# Required only for login and the original authenticated SaaS workflow.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=

# The UI now checks backend capability at runtime. Set this to false only when
# you intentionally want to hide every AI control at build time.
# VITE_AI_ENABLED=false
